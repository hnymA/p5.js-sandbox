function setup() {
    createCanvas(640, 480);

    // アルファ値を記述することで
    // 透明度を指定する
    // アルファ値は最後に記述する

    background(255, 0, 0, 127);

    // colorMode(HSB, 100);
    // background(0, 100, 100, 50);

    // background('rgba(255, 0, 0, 0.5)');
}
