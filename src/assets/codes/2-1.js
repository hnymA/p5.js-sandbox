function setup() {
    createCanvas(640, 480);

    // background(数字）とすることで
    // グレースケールで色の指定を行う
    // 0 - 255

    background(0);   // 黒色
    // background(127); // 灰色
    // background(255); // 白色
}
